node_modules/

# `npm run sync` copies the compiled server and the UI export in here.
server/

# electron-builder output
release/

# Compiled by `npm run desktop` from native/calendar/main.swift.
build/boxaide-calendar
build/boxaide-calendar.arm64
build/boxaide-calendar.x64
